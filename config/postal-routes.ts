import type { PostalRoute } from "@/types/campaign";

/**
 * FINAL USPS ROUTES — not yet selected. Leave empty until carrier routes are
 * locked; the coverage section lists them automatically once added.
 * Phase 2 moves these into the `postal_routes` table.
 */
export const postalRoutes: PostalRoute[] = [];
